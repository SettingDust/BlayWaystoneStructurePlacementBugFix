{
    "schemaVersion": 1,
    "id": "${id}",
    "version": "${version}",
    "name": "${name}",
    "description": "${description}",
    "authors": [
        "${author}"
    ],
    "contact": {
        "homepage": "${source}",
        "sources": "${source}",
        "issues": "${source}/issues"
    },
    "license": "MIT",
    "icon": "assets/${id}/icon.png",
    "environment": "*",
    "mixins": [
        "${id}.mixins.json"
    ],
    "custom": {
        "modmenu": {
            "badges": [
                "library"
            ]
        }
    },
    "entrypoints": {
        "mixinsquared-adjuster": [
            "${group}.BlayWaystoneAnnotationAdjuster"
        ],
        "mixinsquared": [
            "${group}.BlayWaystoneMixinCanceller"
        ],
        "fabric-datagen": [
            "${group}.datagen.Entrypoint"
        ]
    }
}
